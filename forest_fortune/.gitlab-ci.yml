deploy:
  stage: deploy
  before_script:
    - mkdir -p ~/.ssh
    - echo "$STAGGINGGAME_PEM" > ~/.ssh/stagginggame.pem
    - chmod 600 ~/.ssh/stagginggame.pem
  script:
    - ssh -o StrictHostKeyChecking=no -i ~/.ssh/stagginggame.pem ubuntu@15.206.91.228 "cd /home/ubuntu && bash forest.sh"
